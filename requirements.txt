# Core dependencies
fire==0.7.0
PyYAML==6.0.2
transformers>=5.0,<6.0  # Qwen3.5 / qwen3_5 model type lands in 5.x
accelerate
torch
tqdm

scipy>=1.7.0
scikit-learn>=1.0.0

# LLM-as-judge classification (for deconfounded extraction)
google-genai
python-dotenv
openai
peft
bitsandbytes

# Kimi K2 / DeepSeek-V3 architecture support
autoawq
compressed-tensors
tiktoken

# Modal serverless GPU (live-chat backend on Railway)
modal

# MkDocs site (built at deploy time, served at /docs)
mkdocs>=1.6  # exclude_docs requires 1.6+
mkdocs-material
