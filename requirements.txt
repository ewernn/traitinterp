# Core dependencies
fire==0.7.0
PyYAML==6.0.2
transformers==4.57.3
accelerate
torch
tqdm

# traitlens dependencies (now bundled in traitlens/)
scipy>=1.7.0
scikit-learn>=1.0.0

# LLM-as-judge classification (for deconfounded extraction)
google-genai
python-dotenv
openai
peft
bitsandbytes

# Kimi K2 / DeepSeek-V3 architecture support
autoawq
compressed-tensors
tiktoken

# MkDocs site (built at deploy time, served at /docs)
mkdocs
mkdocs-material
