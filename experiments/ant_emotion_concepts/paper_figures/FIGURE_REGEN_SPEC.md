# Figure Regeneration Spec

Per-figure feedback from review. Apply consistently across all figures.

## Global style — APPLY TO EVERY FIGURE
- ±0.08 y-axis for all cosine similarity plots
- Thicker lines (linewidth=2.5)
- DejaVu Sans font (matplotlib default) is fine
- **ALL titles must be coral (#c44e52)** — many figures currently have black titles
- **All font sizes need to be ~2x larger than current** — figures are viewed at 50% width in side-by-side, so text that looks fine standalone becomes unreadable
- Clean spines (no top/right)
- Endpoint labels on line plots (emotion name at last data point)
- Larger dots on scatter plots (s=80+ for side-by-side readability)

## Comparison Table
- Current plain markdown table is boring
- Option A: Expandable rows (click metric name → 1-sentence explanation + which figure)
- Option B: Visual grouped bar chart (Sonnet blue, Llama coral, side by side per metric)
- Option C: Scorecard grid with colored status indicators
- Whichever we pick, each metric should have a tooltip or expandable explanation of what it measures
- "Does not transfer" rows should be visually distinct (red/orange highlight)

## Per-figure fixes needed

### 1. Table 1 — Needs minor fix
- Text list format with ↑/↓ arrows (not grid table) ✓
- L49 probes ✓
- Tight intra-trait spacing ✓
- **TODO**: larger font for the ↑/↓ token lines (currently too small in side-by-side view)

### 2. Fig 2 — Needs fix
- Shrink height to 2/3 of current

### 3. Fig 3 — Needs minor fix
- Only paper's 4 probes ✓
- ±0.08 y-axis ✓
- Thicker lines ✓
- **TODO**: 15% shorter (figsize height ~11 instead of ~13)

### 4. Fig 5 — Needs minor fix
- Block structure visible ✓
- **TODO**: 20% smaller overall
- **TODO**: label font 4px larger
- **TODO**: x-axis labels at 45 degrees (currently 90 degrees, paper uses 45)

### 5. Fig 6 — Needs significant fix
- **Color matching**: Map our clusters to paper's categories by emotion membership overlap. Use the SAME colors as paper:
  - Anthropic colors: green=Exuberant Joy, cyan=Peaceful Contentment, blue=Compassionate Gratitude, purple=Playful Amusement, pink=Competitive Pride, yellow-green=Depleted Disengagement, grey=Vigilant Suspicion, orange=Hostile Anger, brown=Fear and Overwhelm, red=Despair and Shame
  - Only match colors for clusters that have a clear paper counterpart; give unique colors to novel clusters
- **Layout**: Rotate/reflect UMAP to fill white space (currently big gap between left and right clusters). UMAP coords are arbitrary.
- **Zoom**: Much tighter axis limits — too zoomed out currently
- **Font**: Much larger labels (currently unreadable in side-by-side view)
- **Legend**: Move to top-right (paper has top-left, but ours has space top-right). Single column like paper, not 2-column grid.
- **Dot size**: Larger to match paper

### 6. Fig 7 — Needs fix
- Shrink height
- Fix bunched labels (show every 5th, rotated)
- **TODO**: font 4px larger throughout (axis labels, tick labels, title)

### 7. Fig 8 — Needs fix
- Dots 2x larger
- All text 2x larger (labels, axes, title, r value)
- Title should be coral (#c44e52) not black — this applies to ALL figures globally
- Currently generated by the old gen script, not using the Anthropic style consistently

### 8. Fig 9 — OK

### 9. Fig 57 — Needs fix
- Much larger font (currently unreadable in side-by-side)
- Much larger dots
- Paper's fig57.png now saved for side-by-side (wasn't there before)
- Their figure isn't showing up in the viz — check the path in the .md
- Color by cluster, matching Fig 6 colors

### 10. Fig 10 — Needs fix
- Larger dots (s=60+)
- Only 6 traits (Happy, Calm, Loving, Sad, Afraid, Angry) matching paper

### 11. Fig 12 (context prefix) — Needs fix AGAIN
- Should be LINE CHART "Mean Difference by Layer Range" (paper's bottom panel)
- Currently showing the HEATMAP (wrong — got overwritten)
- Two lines: "Early → Early-Mid" (blue) and "Mid-Late → Late" (red)
- X-axis = token position, Y-axis = delta cosine similarity

### 12. Fig 13 (Tylenol) — Needs fix
- Half current height (landscape heatmap)

### 13. Fig 14 (negation) — DONE
- Multi-layer line chart with cosine similarity values

### 14. Fig 15 (person binding) — Needs fix
- Multi-layer line chart ✓
- Cosine similarity values ✓
- **TODO**: too tall — match paper's wider/shorter aspect ratio
- **TODO**: legend below graph in larger box (not inside plot area)
- **TODO**: title is black, should be coral (#c44e52)
- **TODO**: all font larger (global issue)

### 15. Fig 36 (post-training correlation) — Needs fix
- Same global issues: dots too small, font too small, zoomed out
- The non-replication IS interesting (r=0.55 vs 0.90) — RLHF fingerprints differ
- Caption should explain WHY it doesn't replicate more clearly
- Cosine values (~0.5 on ours) are a different scale than paper (~0.04) because paper uses probe activation divided by residual norm, we use cosine similarity. The SHAPE matters, not absolute values.

### 16-18. Figs 37-39 (per-prompt shifts) — Needs fix
- Same global issues: dots too small, font too small
- **Red bars should sort descending** (largest negative diff at bottom, so bar chart reads top-to-bottom as most-increased to most-decreased)
- Currently red bars seem unsorted or sorted wrong direction
- Shape of bar charts is what matters for comparison

### 19. Fig 57 ordering — move earlier in .md (after Fig 9)

### 20. Fig 84 — REMOVED
