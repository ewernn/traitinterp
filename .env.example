# Copy to .env and fill in values
# cp .env.example .env

# -- Required: R2 cloud storage (for downloading experiment data) --
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=
R2_BUCKET_NAME=trait-interp-bucket

# -- Optional: only needed for running extraction/inference/steering --
HF_TOKEN=
OPENAI_API_KEY=
